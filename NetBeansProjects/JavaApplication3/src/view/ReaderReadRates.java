package view;

public enum ReaderReadRates {  // GUI allowed rates, reader can support other values even zero (read continuously) - called AutoStart pause in reader
    readerRate250ms, readerRate500ms, readerRate1sec
}
